import React from 'react'

export default ( ) => {

    return (
        <h1>This is the blog category</h1>
    )
}

// export const query = graphql`
//     query ($id: String) {
//         post: sanityRecipe(id: {eq: $id}) {
            
//         }
//     }
// `