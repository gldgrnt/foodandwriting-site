import React from 'react'

export default ( ) => {

    return (
        <h1>This is the recipe category page</h1>
    )
}

// export const query = graphql`
//     query ($id: String) {
//         post: sanityRecipe(id: {eq: $id}) {
            
//         }
//     }
// `